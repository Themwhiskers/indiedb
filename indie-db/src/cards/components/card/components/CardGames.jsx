import { CardContent, Chip } from "@mui/material";

const CardGames = () => {
    return (
        <CardContent>
            <Chip label='Bastion'/>
        </CardContent>
    );
};

export default CardGames;